<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tn.esprit.freelance">

    <!-- Permissions required for the app -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/> -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity"
            android:exported="true"
            android:label="Register" />
        <activity android:name=".DashboardActivity" />

        <!-- Define the AdminUserManagementActivity here -->
        <activity android:name=".AdminUserManagementActivity"
            android:exported="true"
            android:label="Admin User Management" />

        <!-- Define the UserDetailActivity here -->
        <activity android:name=".UserDetailActivity"
            android:exported="true"
            android:label="User Details" />

        <activity android:name=".ApplyJobActivity" />
        <activity android:name=".ApplicationListActivity"/>
        <activity android:name=".DetailActivity"/>
        <activity android:name=".CandidatListActivity"/>
        <activity android:name=".EditApplicationActivity"/>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="tn.esprit.freelance.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>


</manifest>
